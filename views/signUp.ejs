<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Signup</title>
    <style>
      body {
        background-color: #1a2a6c; /* nebula blue */
        color: white;
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      /* form {
        text-align: center;
      } */

      label {
        display: block;
        margin-top: 10px;
      }

      input,
      select,
      button {
        padding: 8px;
        margin-top: 5px;
        border: none;
        border-radius: 4px;
      }

      input,
      select {
        width: 200px;
      }

      button {
        background-color: #4a00e0;
        color: white;
        cursor: pointer;
        margin-top: 15px;
      }
      </style>
  </head>
  <body>
    <form action="/signup" method="POST">
      <label>Email:</label>
      <input type="email" name="email" required /><br /><br />

      <label>Password:</label>
      <input type="password" name="password" required /><br /><br />

      <label>Tenant:</label>
      <select name="tenant" required>
        <option value="Acme">Acme</option>
        <option value="Globex">Globex</option></select
      ><br />

      <button type="submit">Sign Up</button>
    </form>
  </body>
</html>
