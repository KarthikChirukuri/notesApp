<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Loogin Page</title>
    <style>
      body {
        background-color: #0e1942; /* nebula blue */
        color: white;
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      .Loginbox {
        text-align: center;
      }

      input,
      button {
        padding: 8px;
        margin: 5px 0;
        border: none;
        border-radius: 4px;
      }

      input {
        width: 200px;
      }

      button {
        background-color: #4a00e0;
        color: white;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <div class="Loginbox">
      <form action="/login" method="POST">
        <input
          type="email"
          placeholder="Enter Email"
          name="email"
        /><br /><br />
        <input
          type="password"
          placeholder="Enter PassWord"
          name="password"
        /><br /><br />
        <button>submit</button>
      </form>
    </div>
  </body>
</html>
