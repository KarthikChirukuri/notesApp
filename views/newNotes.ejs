<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Notes page</title>
    <style>
      body {
        background-color: #1a2a6c; /* nebula blue */
        color: white;
        font-family: Arial, sans-serif;
        text-align: center;
        margin: 20px;
      }

      h1 {
        margin-bottom: 20px;
      }

      textarea {
        width: 300px;
        height: 150px;
        padding: 10px;
        border-radius: 4px;
        border: none;
        font-size: 14px;
      }

      button {
        margin-top: 15px;
        background-color: #4a00e0;
        color: white;
        border: none;
        padding: 8px 15px;
        border-radius: 4px;
        cursor: pointer;
      }

      button:hover {
        opacity: 0.8;
      }
    </style>
  </head>
  <body>
    <form action="/notes/new" method="POST">
      <h1>Create you new notes here</h1>
      <textarea placeholder="Enter notes here" name="newNotes"></textarea>
      <br />
      <button>submit</button>
    </form>
  </body>
</html>
